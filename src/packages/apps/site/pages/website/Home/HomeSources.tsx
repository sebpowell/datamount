import { Container } from "@/packages/components/ui/Container";
import {
  PageSection,
  PageSectionTitle,
} from "@/packages/components/ui/PageSection";

const HomeSources = () => {
  return (
    <Container>
      <PageSection>Add data source logos...</PageSection>
    </Container>
  );
};

export { HomeSources };
