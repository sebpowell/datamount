// import { Box } from "@/packages/components/ui/Box";

// const PropertyNavigation = ({
//   sections,
// }: {
//   sections: { [key: string]: ReportSectionProps };
// }) => {
//   return (
//     <Box className="sticky top-0 flex min-h-14 items-center  justify-center gap-8 border-b border-black bg-white">
//       {Object.keys(sections).map((section) => {
//         const { menu } = sections[section];

//         return (
//           <Box as="a" href={`#${section}`} key={section}>
//             {menu.title}
//           </Box>
//         );
//       })}
//     </Box>
//   );
// };

// export { PropertyNavigation };
